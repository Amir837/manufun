# Copilot Instructions for Manufun

## Terminal
- Always use `cmd` (never PowerShell)
- Always use `yarn` as the package manager (never npm)

## Project
- React + TypeScript
- UI library: Ant Design (antd) with dark theme
